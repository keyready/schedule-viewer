const { filterDates } = require('./filterDates');
const { compareStrings } = require('./compareCmd');
const { generateDocument } = require('./createDoc');

module.exports = {
    filterDates,
    compareStrings,
    generateDocument,
};
